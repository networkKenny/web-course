CSS有一點像一個設定。它的全名叫做樣式階層表。
大致上可以分成兩個區域：
html{           選擇器，可以選擇html檔案的標籤
    display:block;      設定區，對選擇到的html標籤設定樣式
    background:red;
}
!注意，每個選擇器只能有一個{}，不過{}裡面想放多少設定就放多少，不過記得每個設定後面都要加;


CSS要寫在哪裡？

(參考影片)

